import { HttpHeaders } from '@angular/common/http';
export declare const mergeHeaders: (source: HttpHeaders, destination: HttpHeaders) => HttpHeaders;
export declare function prioritize<T>(first: T, second: T, init: T): T;
